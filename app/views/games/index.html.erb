<div class="container">
  <h1>See all the games here</h1>
  <ul>
    <% @games.each do |game| %>
    <li>
      <p> <%= link_to game.name, game_path(game) %><%= link_to "rent", new_user_session_path, :class => "btn btn-warning" %> </p>
    </li>
    <% end %>
  </ul>
</div>
